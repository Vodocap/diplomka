[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*

# Playwright settings
addopts = --headed --browser chromium -v
# Use --headed to see browser, remove for CI/headless runs
# Add --screenshot on to capture screenshots on failure

# For fully headless: addopts = --browser chromium -v
